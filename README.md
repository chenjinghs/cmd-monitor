# cmd-monitor

Terminal Monitor + IM Bridge: 监控终端状态，通过飞书/微信双向通信。

## 安装

```bash
pip install -e .
```

## 使用

```bash
cmd-monitor --help
cmd-monitor start
cmd-monitor stop
cmd-monitor status
```

## 开发

```bash
pip install -e ".[dev]"
pytest
```
